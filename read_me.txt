Directory information:

lib: findj-jcel-bin.jar is the binary code of jcel, and we have modified it.

src: The sorce code of our method. Sorry for no more guide document
     and annotation for the code because of the limited time.
     We will soon update it with more detail files.  

     The main test script for our method is EDGSamplesTest.java,
     and BlackboxSamplesTest.java for black-box method.

     In package kse.findj.blackbox is all the source files for black-box method.
     In package kse.findj.edg.core is all the source files for our method.

     